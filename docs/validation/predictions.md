# Stage 1 predictions — pre-registered hypotheses

> Part of the [Stage 1 plan](stage1-plan.md). Every assumption the repository makes about real GitHub behavior, registered **before** any validation ran. Rows are immutable once registered; only the **Disposition** column changes, and only with a matching [observation-log](observation-log.md) entry. Dispositions: `pending` → `confirmed` | `doc-fix` | `code-fix` | `not-observable` (rationale required).
>
> Registered 2026-07-29. Anchors are `file:line` at branch point; they identify the claim, not a promise the line number survives edits.

| ID | Assumption under test | Anchor | Pre-registered prediction | Falsified looks like | Task | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| H1 | GitHub accepts the hand-rolled App JWT: PKCS#1 PEM via `createPrivateKey`, `iat` backdated 60 s, `exp` +8 min, `iss` = numeric App ID as string | `apps/worker/src/github/app-jwt.ts:11-25` | Token exchange returns 201 on first attempt | 401 on `POST /app/installations/{id}/access_tokens` | T5 | confirmed ([log](observation-log.md), 2026-08-01) |
| H2 | Token response is `{token, expires_at}` and the `token <t>` authorization scheme is accepted on subsequent calls | `apps/worker/src/github/client.ts:84-93,209-216` | Parse succeeds; next authorized call is 200 | Shape mismatch, or 401 on the follow-up call | T5 | confirmed ([log](observation-log.md), 2026-08-01) |
| H3 | Artifact listing: bounded pagination (10×100) terminates correctly; `expired` flag semantics match the model | `apps/worker/src/github/client.ts:104-123` | Real listing returns the `test-results` artifact, one page | Missing artifacts; wrong loop exit; unexpected `expired` | T5 | confirmed ([log](observation-log.md), 2026-08-01) |
| H4 | Artifact zip download: fetch follows the 302 to blob storage and drops the auth header cross-origin on its own (comment-only assumption, never exercised) | `apps/worker/src/github/client.ts:135-136` | Download streams to disk; zip is valid | 403 from blob storage; hung or truncated download | T5 | confirmed ([log](observation-log.md), 2026-08-01) |
| H5 | upload-artifact v7 zip layout (nested `apps/*/test-results/junit.xml`, `packages/*/…`) is found by the zip scan | `apps/worker/src/pipeline/artifact-stage.ts`; `.github/workflows/ci.yml:81-86` | `processing_status = 'succeeded'`, all suites' cases persisted | `no_artifacts` or `failed` on a run that uploaded results | T5 | confirmed ([log](observation-log.md), 2026-08-01) |
| H6 | Real `workflow_run` payloads match normalization: nullable `head_branch`, conclusion enum incl. values the stub never sent (`cancelled`, `skipped`, `startup_failure`), numeric IDs at real magnitudes | `apps/worker/src/pipeline/normalize-run.ts` | Real deliveries normalize without permanent failures | Worker permanent-fails a real delivery; wrong normalized fields vs the GitHub UI | T4–T5 | confirmed ([log](observation-log.md), 2026-08-01) |
| H7 | Real Checks API accepts the neutral POST/PATCH body; returned ID round-trips through `flake_check_run_id`; PATCH lands on the same check | `apps/worker/src/github/client.ts:148-196`; ADR-0011 | Exactly one check on the PR; DB ID = ID in the check URL; evidence table renders | 422 on create; duplicate checks; ID mismatch | T6 | pending |
| H8 | Checks-permission-pending 403 is transient: retries into DLQ while ingestion and scoring continue (guide's documented behavior) | `docs/github-app-setup.md:37`; `apps/worker/src/errors.ts` | Only observable if permission approval lags installation | Job classified permanent; ingestion blocked | T6 (opportunistic) | pending |
| H9 | Auth.js on the App's own OAuth credentials works against real GitHub with the `127.0.0.1:3001` callback | `apps/api/src/auth/authjs-plugin.ts`; guide §3b | Login round-trip yields a session row; `/api/v1/me` resolves | Callback mismatch; login loop; no session persisted | T7 | pending |
| H10 | Signed-state Setup-URL claiming survives the real post-install redirect (incl. "Redirect on update") | `apps/api/src/github/install-state.ts`; ADR-0012 | Installation claimed by the workspace after a real install click | Installation stays unclaimed; state rejected | T7 | pending |
| H11 | Installation lifecycle events (`installation`, `installation_repositories`) arrive as configured and keep `installations` rows in sync | `apps/worker/src/pipeline/installation-event.ts`; guide §3b | Rows appear/update on install, repo-selection change, uninstall | Missing or unhandled lifecycle deliveries | T4, T7 | pending |
| H12 | (Environment) WSL2 clock drift after host sleep does not invalidate freshly minted JWTs in practice; the 60 s backdate absorbs it | `apps/worker/src/github/app-jwt.ts:16` | No 401 bursts across the stage's sessions | Intermittent token-exchange 401s correlating with host sleep | all (passive) | pending |
| H13 | Real redelivery converges: the Redeliver button on a real delivery is absorbed as a duplicate, one row per GUID | ADR-0005; `apps/api/src/routes/webhooks.ts` | Second delivery logs `duplicate webhook delivery absorbed`; row count unchanged | Duplicate `webhook_events` rows | T4 | confirmed ([log](observation-log.md), 2026-07-31) |
