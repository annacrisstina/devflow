# v0.1.0 release checklist

> The [roadmap](../project-memory/roadmap.md#mvp-definition-release-gate-for-v010) MVP gate, clause by clause, with the evidence behind each claim. Engineering rows are verified; founder rows are the real-GitHub steps only the founder can run (App credentials, live deployment).

## Where the release sequence actually stands (recorded 2026-07-28)

The tag went out without the steps behind it completing in order. The honest state, step by step:

- Steps 1–2 **✅ done**: PR #13 merged; annotated tag `v0.1.0` created 2026-07-20 on the merge commit `8128889` and pushed to origin.
- Step 3 **⏸ intentionally deferred** (founder ruling, 2026-07-28): no GitHub Release exists yet. It will be created from the CHANGELOG 0.1.0 section **after Stage 1 of v0.1.x** (first real-GitHub validation), so the Release announces something validated against the real API, not only against the stub.
- Step 4 **→ v0.1.x Stage 1**: the real-GitHub pass has not run — `.env` carries only the webhook secret; the App has never been configured. This step _is_ the current phase's first gate, not a leftover.
- Step 5 **✖ not done**: no demo video exists. Deferred alongside the Release.
- Step 6 **✖ not started**: the Dependabot majors queue (7 open branches on origin) — a mandatory v0.1.x item per D-M6-6/D13.
- The two 🔶 gate rows above (live PR annotation, quarantine click-through in the real UI) remain open and are v0.1.x Stage 1/2 exit evidence.

## Updates — Stage 1 closeout (2026-08-06 → 2026-08-07)

- Step 4's real-GitHub pass **has now run**: Stage 1 validation complete ([report](../validation/validation-report.md)) — App configured per §3b, login/claim/ingestion/detection/annotation/quarantine all exercised against real GitHub. Both founder gate rows in the table are flipped ✅ with observation-log evidence.
- Steps 3 (GitHub Release) and 5 (demo video) are **unblocked** — per the founder ruling, Stage 1 + Release + demo video is the portfolio-presentable milestone.
- Step 6 (Dependabot majors queue) remains open, post-release.
- **2026-08-07:** the stage's final open clause — the live feed under real traffic — closed after a root-cause hunt (the claim-flow Save had silently dropped `devflow-demo-flaky` from the installation; see the log's 2026-08-07 entries). Stage 1 is complete with no open clauses.

| Gate clause                                 | Status         | Evidence                                                                                                                                                                                                                                                                        |
| ------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Install the GitHub App on a repo            | ✅ founder     | Real App created, installed, and claimed through the live Setup-URL flow ([log](../validation/observation-log.md), 2026-07-30/31 App walk; 2026-08-06 claim — H10/H11 confirmed)                                                                                                |
| Push code → runs ingested                   | ✅ engineering | e2e: signed deliveries → 202 → normalized runs; seed replays 12 runs through the same path                                                                                                                                                                                      |
| JUnit results parsed                        | ✅ engineering | 158-test suite (parser corpus) + e2e + dogfood wiring: vitest JUnit output verified against DevFlow's own parser                                                                                                                                                                |
| Flaky tests detected and scored             | ✅ engineering | e2e pins ADR-0010 arithmetic (0.33 suspected → 0.6 flaky); seed pins decayed variants (0.529 / 0.464 / always-red = 0)                                                                                                                                                          |
| PR check annotates known-flaky failures     | ✅             | Live: neutral check on the real PR Checks tab, DB `flake_check_run_id` = check-URL ID (92586551161), PATCH idempotency held ([log](../validation/observation-log.md), 2026-08-01 create/PATCH + 2026-08-06 on-the-PR — H7 confirmed)                                            |
| Maintainer approves quarantine in dashboard | ✅             | Live: real-UI approval wrote `quarantine_records` row 1 (`active`, real session user, 2026-08-06 14:15Z) ([log](../validation/observation-log.md), 2026-08-06 T7 closed)                                                                                                        |
| Live feed shows activity                    | ✅ engineering | e2e: workspace-scoped socket events for ingest/process/score; observed live 2026-08-07 — Runs page updated unrefreshed on a real rerun ([log](../validation/observation-log.md), "Exit criterion 3 closed")                                                                     |
| Self-hostable with `docker compose up`      | ✅ engineering | Stranger test passed 2026-07-20: fresh clone, guide followed literally with throwaway creds → full profile healthy on first boot (migrate exit 0, SPA served) → seed 6/6, database populated per storyboard. Caught one defect (missing-build failure mode), fixed as `4401817` |

## Release steps (founder — original sequence, kept for reference; current status above)

0. **Pre-tag gate — ✅ completed 2026-07-20** on healed Docker: `pnpm verify` green (203 tests), `pnpm e2e` 21/21, stranger test passed (fresh clone → [self-hosting.md](../self-hosting.md) followed literally → `pnpm install && pnpm build` → `pnpm demo:seed` → populated database). Re-running any leg before tagging is optional reassurance, not a requirement.
1. Merge the PR; confirm CI green on `main` (the dogfood artifact appears on the run).
2. `git tag -a v0.1.0 -m "DevFlow v0.1.0"` on the merge commit; `git push origin v0.1.0`.
3. Create the GitHub Release from the tag; body = the 0.1.0 section of [CHANGELOG.md](../../CHANGELOG.md).
4. Real-GitHub pass (standing since M4): reconfigure the App (§3b), log in, claim, install on this repo + `devflow-demo-flaky`, watch ingestion live.
5. Record the demo video ([storyboard](../../scripts/demo/README.md)).
6. After the tag: work the Dependabot majors queue, individually (D-M6-6).
